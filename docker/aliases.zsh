# add shortcut for docker-machine commands
alias dm="docker-machine"
alias dup="docker-machine start default"

# add shortcut for docker-compose
alias dc='docker-compose'

# add shortcut for docker-machine
alias dm='docker-machine'

# alias for starting docker
alias dup='docker-machine start default && eval "$(docker-machine env default)"'
